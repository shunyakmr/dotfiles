# Lists of Function

npp(){
	local realfile
	realfile=$(readlink -f "$1")
	"/mnt/c/Program Files/Notepad++/notepad++.exe" "$(wslpath -w "$realfile")"
}

gpub(){
	git add .
	git commit -m "$1"
	git push
}


